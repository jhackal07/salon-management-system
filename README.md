
# Salon Management System

Full-stack salon booking system.

Frontend:
Angular

Backend:
Node.js + Express

Database:
PostgreSQL

Run Backend:

cd salon-backend
npm install
node server.js

Frontend: full Angular app in `salon-frontend/salon-app` (services and pages per folder). Run: `cd salon-frontend/salon-app && npm install && npm start`
